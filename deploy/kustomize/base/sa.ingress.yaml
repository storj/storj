---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: satellite-ing
  annotations:
    kubernetes.io/ingress.class: nginx
    # TODO: put behind vpn if we want
    # nginx.ingress.kubernetes.io/whitelist-source-range: <storj vpn>
spec:
  # TODO: add tls with cert-manager and letsencrypt
  # tls:
  # - hosts:
  #   - sa-dev.jessica-us-east1.dev.storj.io
  #   secretName: 
  rules:
  - host: sa-dev.jessica-us-east1.dev.storj.io
    http:
      paths:
      - path: /
        backend:
          serviceName: satellite-svc
          servicePort: 80
